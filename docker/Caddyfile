# Reference-only Caddyfile.
#
# Local docker-compose uses docker/caddy_start.sh to generate a Caddyfile at runtime.
# Azure deployments via scripts/deploy can also generate a Caddyfile internally.

:80 {
	auto_https off
	reverse_proxy localhost:8081
}
